#!/bin/sh

set -e

## GreenLinux Debian Edition (GLDE) — build wrapper.
## Executed automatically by `lb build` (redirect via Maybe_auto_redirect).

if [ ! -e config/common ]
then
	lb config
fi

lb build noauto "${@}"
